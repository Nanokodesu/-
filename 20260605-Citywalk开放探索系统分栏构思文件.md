# Citywalk开放探索系统分栏构思文件

## 01. 文件定位

本文件是`20260605-Citywalk开放探索系统25功能技术文档.md`的界面组织补充文件。

它不重复讲每个功能的代码实现，而是专门回答界面怎么分层、怎么分栏、一级界面有几个、二级界面有几个、是否需要三级标题，以及这些内容如何排布才不会互相遮挡。

系统整体定位是“开放探索型Cesium数字孪生导览平台”，不是线性闯关式页面。

一级界面负责承载用户进入系统后最先看到的主要工作区。

二级界面负责在一级界面内部切换功能组。

三级标题只用于二级界面内部的细分功能，不再形成新的大窗口。

## 02. 总体界面判断

建议采用“1个主场景+5个一级界面区+14个二级界面组+36个三级功能标题”的组织方式。

这里的“一级界面”不是5个独立网页，而是同一个Cesium主页面上的5个稳定区域。

5个一级界面分别是：顶部任务栏、左侧探索栏、右侧分析栏、底部时间线栏、地图浮动工具层。

主场景始终是Cesium三维城市，不作为普通面板计算。

所有功能必须依附在这5个一级界面区内，不能再随意新增漂浮大窗。

二级界面采用标签页、折叠组、抽屉或小型弹层组织。

三级标题采用小标题、按钮组、列表项、图例项表达，不再单独弹出新界面。

这样可以保证功能数量很多，但页面仍然像一个完整平台，而不是几十个窗口堆叠。

## 03. 一级界面数量

一级界面建议为5个。

一级界面A：顶部任务栏。

一级界面B：左侧探索栏。

一级界面C：右侧分析栏。

一级界面D：底部时间线栏。

一级界面E：地图浮动工具层。

这5个一级界面同时存在，但默认并不全部展开。

顶部任务栏常驻，承担系统入口、搜索、AI语音和全局状态。

左侧探索栏默认展开，承担Citywalk开放探索和节点导览。

右侧分析栏默认半收起，用户点击分析或课程按钮后展开。

底部时间线栏默认压缩为短条，故事导览或人流回放时展开。

地图浮动工具层默认只显示一列小图标，不显示文字面板。

## 04. 一级界面排布

整体页面采用全屏Cesium底图。

顶部任务栏固定在页面上方，高度建议56px至64px。

左侧探索栏固定在左侧，宽度建议320px至360px。

右侧分析栏固定在右侧，宽度建议360px至420px。

底部时间线栏固定在底部，高度默认54px，展开后不超过110px。

地图浮动工具层放在左侧栏与地图之间，或放在右下角垂直排列。

中间三维场景始终保留为最大面积。

左栏和右栏不同时占满高度，避免把视野压成狭窄通道。

顶部栏、左右栏、底部栏之间要留出8px至12px间距。

所有面板使用深色半透明背景，透明度建议0.72至0.86。

边框使用低饱和蓝灰或浅紫灰细线，避免大白框。

## 05. 一级界面A：顶部任务栏

顶部任务栏是全局控制层。

它不承载大段内容，只承载入口、模式、搜索和状态。

顶部任务栏建议包含4个二级界面组。

二级A1：系统标题与场景状态。

二级A2：开放探索模式切换。

二级A3：AI语音与文本搜索入口。

二级A4：全局状态与快捷按钮。

### A1.系统标题与场景状态

三级标题A1-1：系统名称。

三级标题A1-2：当前定位点。

三级标题A1-3：模型加载状态。

系统名称建议写为“武康路Citywalk开放探索数字孪生导览系统”。

当前定位点显示用户正在查看的街区或节点。

模型加载状态只显示短句，不弹出新窗口。

### A2.开放探索模式切换

三级标题A2-1：自由探索。

三级标题A2-2：游客故事。

三级标题A2-3：人流分析。

三级标题A2-4：课程展示。

这4个按钮是系统的主模式入口。

点击后改变左栏或右栏的内容，不改变Cesium主场景。

自由探索是默认模式。

游客故事打开底部时间线。

人流分析打开右侧分析栏。

课程展示打开右侧课程映射标签页。

### A3.AI语音与文本搜索入口

三级标题A3-1：语音按钮。

三级标题A3-2：文本输入框。

三级标题A3-3：意图识别结果。

三级标题A3-4：一键飞行按钮。

语音按钮只占一个圆形图标。

文本输入框宽度不宜过长，建议260px至320px。

识别结果不使用聊天大窗，只在顶部栏和左侧卡片同步显示。

一键飞行按钮触发`viewer.camera.flyTo`或`flyToBoundingSphere`。

### A4.全局状态与快捷按钮

三级标题A4-1：重置视角。

三级标题A4-2：图层抽屉。

三级标题A4-3：全屏模式。

三级标题A4-4：帮助说明。

帮助说明只打开小型说明层，不要做大段教程页。

图层抽屉建议从右侧滑出，但宽度不超过320px。

## 06. 一级界面B：左侧探索栏

左侧探索栏是系统最重要的用户操作区。

它承担开放探索、节点发现、路线选择和游客故事说明。

左侧探索栏建议包含4个二级界面组。

二级B1：探索总览。

二级B2：兴趣点分类。

二级B3：路线与节点。

二级B4：游客故事卡片。

### B1.探索总览

三级标题B1-1：今日推荐。

三级标题B1-2：当前范围。

三级标题B1-3：探索提示。

三级标题B1-4：热度摘要。

探索总览用于告诉用户“现在能看什么”。

它应该短小，不做长篇文字。

今日推荐最多显示3个节点。

当前范围显示武康路、衡复风貌区或徐汇模型范围。

热度摘要只显示一行，如“当前高热节点：武康大楼、安福路口”。

### B2.兴趣点分类

三级标题B2-1：历史建筑。

三级标题B2-2：咖啡与商业。

三级标题B2-3：拍照打卡。

三级标题B2-4：文化展馆。

三级标题B2-5：慢行休憩。

兴趣点分类使用按钮组或复选框。

每次点击只更新地图点位和左栏列表。

不要为每个分类弹出独立面板。

被选中的分类在地图上使用颜色区分。

### B3.路线与节点

三级标题B3-1：经典漫游线。

三级标题B3-2：避拥挤替代线。

三级标题B3-3：建筑阅读线。

三级标题B3-4：夜间Citywalk线。

三级标题B3-5：自定义临时路线。

路线列表放在左栏中部。

点击路线后地图显示线和节点，不弹出路线大图。

每条路线卡片包含名称、预计时长、拥挤程度和一键进入。

路线节点不超过8个，避免地图标签过密。

### B4.游客故事卡片

三级标题B4-1：人物身份。

三级标题B4-2：故事段落。

三级标题B4-3：下一节点。

三级标题B4-4：跳过故事。

游客故事是可选模块，不是系统主线。

故事卡片只在用户进入游客故事模式后出现。

卡片最多显示3行正文。

更长的叙事进入底部时间线，不放在左栏。

## 07. 一级界面C：右侧分析栏

右侧分析栏承担课程技术展示和数据分析。

它不适合放游客操作入口，而适合放图表、图层、统计、课程映射。

右侧分析栏建议包含4个二级界面组。

二级C1：人流热力分析。

二级C2：建筑与空间分析。

二级C3：图层与数据管理。

二级C4：课程功能映射。

### C1.人流热力分析

三级标题C1-1：实时热力。

三级标题C1-2：分时热力。

三级标题C1-3：拥挤预警。

三级标题C1-4：避让建议。

热力图透明度不超过0.55。

右栏展示图例和数值，地图只显示热力面。

拥挤预警使用红橙黄三级，不写长篇解释。

避让建议最多列出3条。

### C2.建筑与空间分析

三级标题C2-1：建筑分类设色。

三级标题C2-2：建筑信息卡。

三级标题C2-3：可视域分析。

三级标题C2-4：缓冲区分析。

建筑点击信息不使用Cesium默认InfoBox。

所有建筑信息统一进入右栏卡片。

可视域分析只保留结果面和短标签。

缓冲区分析结果必须半透明，不能盖住贴图。

### C3.图层与数据管理

三级标题C3-1：3DTiles建筑层。

三级标题C3-2：GeoJSON节点层。

三级标题C3-3：CZML轨迹层。

三级标题C3-4：KML边界层。

三级标题C3-5：热力与分析层。

图层管理默认放右栏，不常驻地图中央。

每一层只提供显示、隐藏、定位、透明度四类操作。

透明度使用滑块。

定位使用小图标按钮。

### C4.课程功能映射

三级标题C4-1：3DTiles加载。

三级标题C4-2：鼠标事件。

三级标题C4-3：绘制与测量。

三级标题C4-4：ECharts联动。

三级标题C4-5：GLSL材质。

三级标题C4-6：粒子与特效。

课程功能映射是展示课程学习成果的关键区域。

它建议做成可折叠清单。

每一项写“课程知识点+系统应用位置+代码对象”。

不要把课程笔记全文搬进页面。

## 08. 一级界面D：底部时间线栏

底部时间线栏用于时间、路线、故事、人流回放。

它默认不展开，避免遮挡地图。

底部时间线栏建议包含2个二级界面组。

二级D1：游客故事时间线。

二级D2：人流时段回放。

### D1.游客故事时间线

三级标题D1-1：故事节点轴。

三级标题D1-2：当前段落。

三级标题D1-3：上一段。

三级标题D1-4：下一段。

故事时间线采用横向节点轴。

每个节点只显示短标题。

正文仍回到左侧游客故事卡片。

地图根据当前节点飞行，不改变其他功能面板。

### D2.人流时段回放

三级标题D2-1：上午。

三级标题D2-2：午后。

三级标题D2-3：傍晚。

三级标题D2-4：夜间。

三级标题D2-5：播放暂停。

时段回放用于驱动热力图和人流轨迹变化。

底栏只放时间控制，不放统计图。

统计图仍在右栏显示。

## 09. 一级界面E：地图浮动工具层

地图浮动工具层只放高频操作按钮。

它不承载长文本。

地图浮动工具层建议包含4个二级界面组。

二级E1：视角工具。

二级E2：绘制测量工具。

二级E3：场景增强工具。

二级E4：沉浸查看工具。

### E1.视角工具

三级标题E1-1：回到总览。

三级标题E1-2：沿街视角。

三级标题E1-3：俯视视角。

三级标题E1-4：跟随路线。

视角工具使用图标按钮。

悬停时显示短提示。

点击后只改变相机，不打开新面板。

### E2.绘制测量工具

三级标题E2-1：距离测量。

三级标题E2-2：面积测量。

三级标题E2-3：临时标注。

三级标题E2-4：清除绘制。

绘制测量属于课程功能展示，也服务开放探索。

测量结果以小标签显示。

长说明写入状态栏。

### E3.场景增强工具

三级标题E3-1：建筑高亮。

三级标题E3-2：热力开关。

三级标题E3-3：卷帘对比。

三级标题E3-4：天气粒子。

这些功能容易遮挡模型，必须默认关闭。

用户打开后要有明显关闭按钮。

卷帘对比只在课程展示或分析模式中使用。

天气粒子适合展示课程特效，不作为主功能常驻。

### E4.沉浸查看工具

三级标题E4-1：全景入口。

三级标题E4-2：视频窗口。

三级标题E4-3：街景照片。

三级标题E4-4：退出沉浸。

沉浸查看属于独立模式。

进入全景或视频后，左右栏应自动收起。

退出后恢复进入前的界面状态。

## 10. 二级界面数量汇总

系统共建议设置18个二级界面组。

顶部任务栏有4个二级界面组。

左侧探索栏有4个二级界面组。

右侧分析栏有4个二级界面组。

底部时间线栏有2个二级界面组。

地图浮动工具层有4个二级界面组。

18个二级界面组足以容纳30个以上功能。

二级界面组之间不要做互相嵌套的大面板。d

二级界面的主形态建议是标签页、折叠组、按钮组和抽屉。

## 11. 三级标题数量汇总

系统建议设置75个左右三级标题。

三级标题不是页面层级，而是功能项层级。

三级标题用于实现时写成小标题、按钮、列表项、图例项或配置项。

三级标题不应产生新窗口。

如果某个三级功能需要更多说明，应写入技术文档或右侧课程映射，不放在地图页面。

## 12. 推荐界面层级树

一级A：顶部任务栏。

二级A1：系统标题与场景状态。

二级A2：开放探索模式切换。

二级A3：AI语音与文本搜索入口。

二级A4：全局状态与快捷按钮。

一级B：左侧探索栏。

二级B1：探索总览。

二级B2：兴趣点分类。

二级B3：路线与节点。

二级B4：游客故事卡片。

一级C：右侧分析栏。

二级C1：人流热力分析。

二级C2：建筑与空间分析。

二级C3：图层与数据管理。

二级C4：课程功能映射。

一级D：底部时间线栏。

二级D1：游客故事时间线。

二级D2：人流时段回放。

一级E：地图浮动工具层。

二级E1：视角工具。

二级E2：绘制测量工具。

二级E3：场景增强工具。

二级E4：沉浸查看工具。

## 13. 页面默认状态

系统打开时默认进入自由探索模式。

顶部任务栏常驻显示。

左侧探索栏展开。

右侧分析栏收起为窄按钮。

底部时间线栏收起为短条。

地图浮动工具层显示图标。

建筑3DTiles直接显示。

节点GeoJSON默认显示核心节点。

路线默认不显示，用户选择后显示。

热力图默认不显示，进入人流分析后显示。

故事导览默认不显示，进入游客故事后显示。

## 14. 交互状态切换

用户点击“自由探索”时，左侧栏显示探索总览、兴趣点分类、路线与节点。

用户点击“游客故事”时，左侧栏切换为故事卡片，底部时间线展开。

用户点击“人流分析”时，右侧栏展开，显示人流热力分析和图表。

用户点击“课程展示”时，右侧栏展开，显示课程功能映射。

用户点击地图建筑时，右侧栏显示建筑信息。

用户点击地图节点时，左侧栏显示节点详情。

用户使用AI搜索时，系统先匹配节点或功能，再执行飞行、开图层或切换模式。

用户进入全景、视频或沉浸模式时，左右栏自动收起。

退出沉浸模式后恢复原先栏位状态。

## 15. 防遮挡规则

规则一：地图中央不放大面板。

规则二：一级面板不超过5个。

规则三：任何新功能必须归入已有一级界面。

规则四：右栏负责分析，左栏负责探索，不互相抢功能。

规则五：底栏只负责时间和回放，不放图表。

规则六：地图工具层只放图标，不放长文字。

规则七：图层抽屉默认收起。

规则八：游客故事不强制覆盖自由探索。

规则九：课程展示不干扰游客使用，只放右栏。

规则十：全景和视频必须有明确退出按钮。

## 16. 推荐排布尺寸

顶部任务栏高度：60px。

左侧探索栏宽度：340px。

右侧分析栏宽度：390px。

底部时间线栏默认高度：54px。

底部时间线栏展开高度：96px。

地图工具按钮尺寸：38px乘38px。

面板圆角：8px。

面板间距：10px。

主按钮高度：32px。

二级标签高度：32px。

三级功能项高度：28px至44px。

## 17. 视觉风格建议

整体使用深色透明城市仪表盘风格。

背景色建议使用`rgba(11,16,26,0.78)`。

一级面板边框建议使用`rgba(159,183,255,0.22)`。

重点按钮使用蓝紫色或青蓝色。

警示信息使用橙红色。

普通文本使用浅灰白。

次级文本使用蓝灰。

地图点位使用分类色，但不要超过6种主色。

路线线条使用高饱和但细线表达。

热力图必须半透明。

## 18. 功能与界面归属表

| 功能类别 | 功能名称 | 一级界面 | 二级界面 | 三级标题形式 |
| :--- | :--- | :--- | :--- | :--- |
| 底座 | 3DTiles建筑加载 | 右侧分析栏 | 图层与数据管理 | 图层项 |
| 底座 | 本地Cesium资源状态 | 顶部任务栏 | 系统标题与场景状态 | 状态短句 |
| 探索 | 兴趣点分类筛选 | 左侧探索栏 | 兴趣点分类 | 按钮组 |
| 探索 | 节点详情查看 | 左侧探索栏 | 路线与节点 | 信息卡 |
| 探索 | 路线选择 | 左侧探索栏 | 路线与节点 | 路线卡 |
| 探索 | 自定义临时路线 | 左侧探索栏 | 路线与节点 | 操作按钮 |
| 导览 | 游客故事段落 | 左侧探索栏 | 游客故事卡片 | 叙事卡 |
| 导览 | 故事时间线 | 底部时间线栏 | 游客故事时间线 | 横向节点 |
| AI | 语音跳转 | 顶部任务栏 | AI语音与文本搜索入口 | 图标按钮 |
| AI | 文本搜索 | 顶部任务栏 | AI语音与文本搜索入口 | 输入框 |
| 分析 | 人流热力 | 右侧分析栏 | 人流热力分析 | 图例项 |
| 分析 | 分时回放 | 底部时间线栏 | 人流时段回放 | 时间按钮 |
| 分析 | 建筑信息 | 右侧分析栏 | 建筑与空间分析 | 信息卡 |
| 分析 | 可视域分析 | 右侧分析栏 | 建筑与空间分析 | 操作项 |
| 课程 | ECharts联动 | 右侧分析栏 | 课程功能映射 | 折叠项 |
| 课程 | 绘制测量 | 地图浮动工具层 | 绘制测量工具 | 图标按钮 |
| 课程 | GLSL材质 | 地图浮动工具层 | 场景增强工具 | 开关项 |
| 课程 | 粒子天气 | 地图浮动工具层 | 场景增强工具 | 开关项 |
| 沉浸 | 全景查看 | 地图浮动工具层 | 沉浸查看工具 | 图标按钮 |
| 沉浸 | 视频窗口 | 地图浮动工具层 | 沉浸查看工具 | 图标按钮 |

## 19. 不建议采用的界面方式

不建议把30个功能全部做成一级按钮。

不建议把课程功能做成单独覆盖全屏的页面。

不建议用多个白色弹窗堆在Cesium上。

不建议把游客故事作为唯一主线。

不建议让右栏和左栏同时滚动很长内容。

不建议地图上长期显示大量文字标签。

不建议每个节点都弹出Cesium默认InfoBox。

不建议在底部栏放复杂图表。

不建议把图层面板常驻展开。

不建议让AI对话框变成聊天软件式大窗口。

## 20. 后续实现顺序

第一步实现顶部任务栏和左侧探索栏。

第二步实现右侧分析栏的标签页框架。

第三步把现有30功能归入对应二级界面。

第四步实现图层抽屉和状态同步。

第五步实现游客故事模式和底部时间线。

第六步实现人流热力、ECharts图表和时段回放。

第七步实现AI文本跳转，再扩展语音输入。

第八步实现全景、视频和沉浸模式的自动收起逻辑。

第九步补充课程功能映射清单。

第十步做界面防遮挡和响应式测试。

## 21. 最终结论

本系统推荐不是多页面网站，而是单HTML中的多区域仪表盘。

一级界面建议5个，分别承担全局、探索、分析、时间和工具。

二级界面建议18个，用标签页和折叠组组织功能。

三级标题建议75个左右，只作为功能项，不作为独立页面。

这种结构能同时容纳开放探索、游客故事、人流分析、AI跳转、课程功能展示和沉浸查看。

后续实现时，只要遵守“新功能必须归入既有一级界面”的规则，就不会出现功能互相妨碍、窗口互相遮挡的问题。
