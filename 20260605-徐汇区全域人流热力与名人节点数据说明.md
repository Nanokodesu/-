# 徐汇区全域人流热力与名人节点数据说明

## 1.文件位置

- 数据文件：`Data/citywalk_crowd_heat_xuhui_district.json`
- 接入页面：`课程作业.html`
- 原武康路数据保留：`Data/citywalk_crowd_heat_realistic.json`

## 2.本次扩展目标

本次不再把系统范围局限在武康路。

武康路-安福路仍作为徐汇区Citywalk的重要文化节点和名人故居密集片区。

系统范围扩展为徐汇区全域，覆盖徐家汇源、龙华、徐汇滨江、西岸艺术带、上海南站、漕河泾、上海植物园、衡山路、枫林医疗科研片区、田林生活片区等。

## 3.数据性质

这份数据属于“真实空间节点驱动的片区级估算人流数据”。

它不是官方实时客流、手机信令、景区闸机、摄像头识别或平台定位数据。

它的现实基础来自公开地图要素、真实POI、轨道交通节点、文旅节点、名人故居分布和不同城市活动场景的时间规律。

## 4.为什么采用片区人数

原先热力数据以单个点位百分比表达，适合武康路小范围演示。

徐汇区范围扩大后，如果仍然只用点位百分比，会出现两个问题：

- 热力范围太碎，不能表达徐汇区内部不同功能片区的人流差异。
- 数值不够直观，无法回答“哪个区域有多少人”“哪个片区最拥挤”这类问题。

因此新数据以`regions`作为主热力单位，每个片区在每个时段都有估算人数。

## 5.核心数据结构

```json
{
  "metadata": {},
  "timeSlots": [],
  "regions": [],
  "points": [],
  "corridors": []
}
```

## 6.regions：片区级人流热力

`regions`是当前热力图的主数据。

每个片区包含：

- `id`：片区ID。
- `name`：片区名称。
- `category`：片区类型。
- `center`：片区中心经纬度。
- `radiusMeters`：Cesium椭圆热力范围半径。
- `slots`：五个时段的人数估算。
- `basis`：人数与片区设置依据。

当前共设置12个片区。

## 7.points：名人故居与真实POI节点

`points`不直接作为主热力单位。

它用于在地图上叠加可点击的真实节点，包括名人故居、文化场馆、交通节点、商圈、公园和滨江节点。

当前包含37个节点。

其中武康路-安福路片区保留了巴金故居、宋庆龄故居纪念馆、张乐平旧居、柯灵故居、夏衍旧居等名人节点。

这些节点可支持后续游客故事导览、AI语音跳转和名人路线生成。

## 8.corridors：路线与走廊

`corridors`用于后续路线推荐。

当前设置5类走廊：

- 名人故居Citywalk线
- 徐家汇源文化商业线
- 龙华-植物园日间线
- 西岸滨江夜游线
- 通勤压力线

## 9.页面接入逻辑

页面启动时读取：

```js
fetch("./Data/citywalk_crowd_heat_xuhui_district.json")
```

读取成功后，页面优先使用`regions`生成右侧人流面板和地图热力椭圆。

每个片区的`slots`人数会被转换为0-100强度，用于控制热力颜色和条形宽度。

## 10.颜色规则

- 蓝色：相对舒适片区。
- 黄色：活跃片区。
- 红色：拥挤或高压片区。

颜色依据不是绝对人数，而是当前时段内12个片区的相对强度。

## 11.界面显示规则

右侧人流面板显示当前时段人数最高的12个片区。

地图热力绘制所有片区。

名人故居、文化场馆、交通节点会作为小点叠加，不抢占片区热力主视觉。

## 12.课程汇报口径

可表述为：

“系统基于徐汇区真实POI、文旅节点、轨道交通和功能片区构建片区级估算人流数据。热力图不是官方实时客流，而是数字孪生课程原型中的可运行场景数据，用于展示不同时段徐汇区人流变化、拥挤预警、避让路线和名人故居导览。”

