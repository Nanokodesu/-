#nano 

## 4.1 GLSL 实现原理

![](https://p.ananas.chaoxing.com/star3/origin/45f9f95e9c7f7383e13aa3f0ee76a0a3.gif "流动线示意.gif")

## 4.2 实现效果

![](https://p.ananas.chaoxing.com/star3/origin/8abb11c437d7f3f863e6c92d634417af.gif "动态线.gif")

## 4.3 代码实现
```JavaScript
 (async function () {
    var tileset = await Cesium.Cesium3DTileset.fromUrl(
        "./Data/SHP/Slice_shp/tileset.json",
    );
    viewer.scene.primitives.add(tileset);
    viewer.flyTo(tileset);
    return tileset;
})();

// 使用 ES6 Class 定义材质属性类
class FlowLineMaterialProperty {
    constructor(color, duration) {
        this._definitionChanged = new Cesium.Event();
        this._color = undefined;
        this._time = performance.now();
        this.color = color;
        this.duration = duration;
    }

    get isConstant() { 
        return false; 
    }
    get definitionChanged() { 
        return this._definitionChanged; 
    }
    getType(time) { 
        return 'FlowLineType'; 
    }

    getValue(time, result = {}) {
        result.color = Cesium.Property.getValueOrClonedDefault(this._color, time, Cesium.Color.WHITE, result.color);
        result.image = "./Texture/TrailLine.jpg"; 
        result.time = ((performance.now() - this._time) % this.duration) / this.duration;
        return result;
    }

    equals(other) {
        return this === other || (other instanceof FlowLineMaterialProperty && Cesium.Property.equals(this._color, other._color));
    }
}
// 颜色属性被 Cesium 动画系统追踪
Object.defineProperty(FlowLineMaterialProperty.prototype, 'color', Cesium.createPropertyDescriptor('color'));

// 注册材质和着色器
Cesium.Material._materialCache.addMaterial('FlowLineType', {
    fabric: {
        type: 'FlowLineType',                                     // 与定义的材质属性类型一致
        uniforms: {                                               // 与定义的材质属性类型中 getValue 方法返回的属性一致
            color: new Cesium.Color(0.0, 0.0, 0.0, 1),
            image: Cesium.Material.DefaultImageId,
            time: 0,
        },
        source: `
    czm_material czm_getMaterial(czm_materialInput materialInput) {
        czm_material material = czm_getDefaultMaterial(materialInput);
        vec2 st = materialInput.st;
        vec4 colorImage = texture(image, vec2(fract(3.0 * st.s - time), st.t));
        material.alpha = colorImage.a * color.a;
        material.diffuse = (colorImage.rgb + color.rgb) / 2.0;
        return material;
    }`
    },
    translucent: () => true
});

// 添加实体并使用自定义材质
const line = viewer.entities.add({
    polyline: {
        positions: Cesium.Cartesian3.fromDegreesArray([
            116.3413878388404, 40.00028114560121,
            116.34124090900055, 40.00196047813912,
            116.34171338652321, 40.00200417651908,
            116.341675703021, 40.003569765721515
        ]),
        width: 10,
        material: new FlowLineMaterialProperty(Cesium.Color.TURQUOISE.withAlpha(0.5), 1000),           // 动画周期为1000毫秒
        clampToGround: true
    }
});
viewer.flyTo(line);

```



## 1.1 实现效果
![](https://p.ananas.chaoxing.com/star3/origin/7e038ebf93f6a21e40bd25551b0d8c08.gif "云.gif")

## 1.2 代码实现
- 独立云的添加
- 数值是0——1。如果需要动起来，则需要乘

```JavaScript
(async function () {
    var tileset = await Cesium.Cesium3DTileset.fromUrl(
        "./Data/SHP/Slice_shp/tileset.json",
    );
    viewer.scene.primitives.add(tileset);
})();

// 创建并添加云的集合
const clouds = viewer.scene.primitives.add(
    new Cesium.CloudCollection({
        noiseDetail: 16.0,               // 用于渲染积云的预计算噪声纹理中捕获的细节量，必须是 2 的幂
    })
);

// 添加云
const cloud1 = clouds.add({
    color: new Cesium.Color(1.0, 1.0, 1.0, 1.0),                          // 云层颜色，白色且完全不透明
    position: Cesium.Cartesian3.fromDegrees(116.34083842367593, 40.00234978089876, 250.0),                   // 云层位置
    scale: new Cesium.Cartesian2(1500, 300),                             // 云层的宽度和高度，实际大小，单位米
    maximumSize: new Cesium.Cartesian3(50, 12, 15),                      // 云层的最大宽度、高度和深度，不是实际大小，是内部的结构大小，肯定比较小
    brightness: 1,                                                       // 云层亮度
    slice: 0.36,                                                         // 云层切片，越小边界越清晰，趋近椭圆
});

// 添加云
const cloud2 = clouds.add({
    color: new Cesium.Color(1.0, 1.0, 1.0, 1.0),
    position: Cesium.Cartesian3.fromDegrees(116.3331098979828, 40.00008797222458, 220.0),
    brightness: 1,
    scale: new Cesium.Cartesian2(1500, 250),
    maximumSize: new Cesium.Cartesian3(50, 15, 13),
    slice: 0.3,
});

// 添加云
const cloud3 = clouds.add({
    color: new Cesium.Color(1.0, 1.0, 1.0, 1.0),
    position: Cesium.Cartesian3.fromDegrees(116.34978915574148, 39.99885882577408, 260.0),
    brightness: 1,
    scale: new Cesium.Cartesian2(230, 110),
    maximumSize: new Cesium.Cartesian3(13, 13, 13),
    slice: 0.2,
});

// 添加云
const cloud4 = clouds.add({
    color: new Cesium.Color(1.0, 1.0, 1.0, 1.0),
    position: Cesium.Cartesian3.fromDegrees(116.33710110631974, 40.00420995572155, 300.0),
    brightness: 1,
    scale: new Cesium.Cartesian2(230, 110),
    maximumSize: new Cesium.Cartesian3(15, 13, 15),
    slice: 0.35,
});

// 添加云
const cloud5 = clouds.add({
    color: new Cesium.Color(1.0, 1.0, 1.0, 1.0),
    position: Cesium.Cartesian3.fromDegrees(116.34507858697022, 40.004528618638005, 260.0),
    brightness: 1,
    scale: new Cesium.Cartesian2(1200, 500),
    maximumSize: new Cesium.Cartesian3(30, 20, 17),
    slice: 0.45,
});

// 设置相机位置
const center = Cesium.Cartesian3.fromDegrees(116.33961726283053, 39.990371983671814, 200.0); // 中心点
const heading = Cesium.Math.toRadians(0.0); // 朝向角度（以弧度为单位）
const pitch = Cesium.Math.toRadians(-5.0); // 俯仰角度（以弧度为单位）
const range = 100.0; // 距离目标的距离

viewer.camera.lookAt(
    center,
    new Cesium.HeadingPitchRange(heading, pitch, range),
)

```


## 3.1 实现效果

![](https://p.ananas.chaoxing.com/star3/origin/f9aa679aba57ae0a0cea4f5f2a3fe7f8.gif "粒子天气-雪.gif")

## 3.2 代码实现

- 新建控制不同天气类型的控件
```

	    <div id="Texture" class="toolbar">
        <select id="dropdown" onchange　="edit()">
            <option value="null">默认状态</option>
            <option value="snow">下雪</option>
            <option value="rain">下雨</option>
        </select>
    </div>
    
```


设置控件样式
```
        .toolbar {
            position: absolute;
            top: 10px;
            left: 10px;
            background: rgba(0, 0, 0, 0.5);
            padding: 10px;
            border-radius: 5px;
            color: white;
        }
```
粒子特效设置
```
        // 粒子特效位置
        const position = Cesium.Cartesian3.fromDegrees(116.34083842367593, 40.00234978089876, 1000.0);
        const modelMatrix = new Cesium.Matrix4.fromTranslation(position);
 
        // 下雪天，粒子特效相关函数定义
        const snowGravityScratch = new Cesium.Cartesian3();                             // 实时更新粒子位置
        function snowUpdate(particle) {
            Cesium.Cartesian3.normalize(particle.position, snowGravityScratch);         // 标准化粒子位置
 
            Cesium.Cartesian3.multiplyByScalar(
                snowGravityScratch,
                Cesium.Math.randomBetween(-100.0, -30.0),                              // 随机速度, 负值表示位置下降
                snowGravityScratch
            );
 
            // 粒子位置根据snowGravityScratch速度更新
            Cesium.Cartesian3.add(
                particle.position,
                snowGravityScratch,
                particle.position
            );
        }
 
        // 下雪天，粒子特效相关常量定义
        const snowRadius = 10000.0;                                                     // 下雪范围半径
        const minimumSnowImageSize = new Cesium.Cartesian2(10, 10);                      // 最小雪花尺寸
        const maximumSnowImageSize = new Cesium.Cartesian2(20, 20);                      // 最大雪花尺寸
 
        // 下雪天配置项
        const snowOption = {
            modelMatrix: modelMatrix,
            lifeTime: 15.0,                                                // 粒子生命周期（秒）
            emitter: new Cesium.SphereEmitter(snowRadius),                 // 粒子发射类型，半球发射
            startScale: 0.5,                                               // 粒子生命周期开始时的比例
            endScale: 1.0,                                                 // 粒子生命周期结束时的比例
            image: './Texture/Snow.png',                                  // 粒子图片
            emissionRate: 5000.0,                                          // 每秒发射的粒子数
            startColor: Cesium.Color.WHITE.withAlpha(0.0),                 // 粒子生命周期开始时的颜色
            endColor: Cesium.Color.WHITE.withAlpha(1.0),                   // 粒子生命周期结束时的颜色
            minimumImageSize: minimumSnowImageSize,                        // 最小雪花尺寸
            maximumImageSize: maximumSnowImageSize,                        // 最大雪花尺寸
            updateCallback: snowUpdate,                                    // 每一帧都要调用一次回调函数以更新粒子
        }
 
        // 下雨天，粒子特效相关函数定义
        const rainGravityScratch = new Cesium.Cartesian3();                             // 实时更新粒子位置
        function rainUpdate(particle) {
            Cesium.Cartesian3.normalize(particle.position, rainGravityScratch);         // 标准化粒子位置
 
            Cesium.Cartesian3.multiplyByScalar(
                rainGravityScratch,
                -1000,                                   // 雨水比雪花下降速快，所以负值更小一些
                rainGravityScratch
            );
            // 粒子位置根据rainGravityScratch速度更新
            Cesium.Cartesian3.add(
                particle.position,
                rainGravityScratch,
                particle.position
            );
        }
 
        // 下雨天配置项
        const rainOption = {
            modelMatrix: modelMatrix,
            lifeTime: 15.0,                                                // 粒子生命周期（秒）
            emitter: new Cesium.SphereEmitter(10000.0),                    // 粒子发射类型，下雨范围
            startScale: 1.0,                                               // 粒子生命周期开始时的比例
            endScale: 0.0,                                                 // 粒子生命周期结束时的比例
            image: './Texture/Rain.png',                                   // 粒子图片
            emissionRate: 15000.0,                                          // 每秒发射的粒子数
            startColor: Cesium.Color.WHITE.withAlpha(0.5),                 // 粒子生命周期开始时的颜色
            endColor: Cesium.Color.WHITE.withAlpha(0.98),                   // 粒子生命周期结束时的颜色
            imageSize: new Cesium.Cartesian2(10, 10),                        // 每个雨滴的尺寸
            updateCallback: rainUpdate,                                     // 每一帧都要调用一次回调函数以更新粒子
        }
 
        let snowParticleSystem = null;
        let rainParticleSystem = null;
 
        const dropdown = document.getElementById('dropdown');
        function edit() {
            if (dropdown.value === "null") {
                if (snowParticleSystem) {
                    viewer.scene.primitives.remove(snowParticleSystem);
                    snowParticleSystem = null;
                }
                if (rainParticleSystem) {
                    viewer.scene.primitives.remove(rainParticleSystem);
                    rainParticleSystem = null;
                }
            } else if (dropdown.value === "snow") {
                if (rainParticleSystem) {
                    viewer.scene.primitives.remove(rainParticleSystem);
                    rainParticleSystem = null;
                }
                snowParticleSystem = new Cesium.ParticleSystem(snowOption);
                viewer.scene.primitives.add(snowParticleSystem);
            } else if (dropdown.value === "rain") {
                if (snowParticleSystem) {
                    viewer.scene.primitives.remove(snowParticleSystem);
                    snowParticleSystem = null;
                }
                rainParticleSystem = new Cesium.ParticleSystem(rainOption);
                viewer.scene.primitives.add(rainParticleSystem);
            }
        }
```


## 4.1 实现效果
![[附件/Pasted image 20260420104511.png]]
## 4.2 代码实现
```javascript
        // 粒子起点，发射源
        const staticPosition = Cesium.Cartesian3.fromDegrees(116.33915181764083, 40.0059357901581, 0);
 
        // 将粒子系统的模型坐标系转变为世界坐标系的矩阵
        const modelMatrix = Cesium.Transforms.eastNorthUpToFixedFrame(staticPosition);
 
        // 粒子发射器的模型矩阵
        function computeEmitterModelMatrix() {
            const hpr = new Cesium.HeadingPitchRoll.fromDegrees(0.0, 0.0, 0.0);              // 粒子发射器的方向、俯仰角、滚动角
            const trs = new Cesium.TranslationRotationScale();                                                        // 定义一个位移、旋转、缩放
            trs.translation = Cesium.Cartesian3.fromElements(0.0, 0.0, 0.0);                 // 发射器在x y z轴的平移位置
            trs.rotation = Cesium.Quaternion.fromHeadingPitchRoll(hpr);                      // 不旋转
            return Cesium.Matrix4.fromTranslationRotationScale(trs, new Cesium.Matrix4());         // 返回一个模型矩阵;
        }
        
        viewer.scene.primitives.add(
            new Cesium.ParticleSystem({
                image: "./Texture/Fire.png",
                startColor: Cesium.Color.RED.withAlpha(0.8),
                endColor: Cesium.Color.YELLOW.withAlpha(0.3),
                startScale: 2.0,
                endScale: 6.0,
                minimumParticleLife: 1.0,
                maximumParticleLife: 4.0,
                lifetime: 180.0,
                speed: 5.0,
                imageSize: new Cesium.Cartesian2(10, 10),
                emitter: new Cesium.CircleEmitter(10.0),
                emissionRate: 20.0,
                modelMatrix: modelMatrix, 
                emitterModelMatrix: computeEmitterModelMatrix(),
            })
        ); 
```

AI代码有几大特征？