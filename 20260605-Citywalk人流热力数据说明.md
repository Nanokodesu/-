# Citywalk人流热力数据说明

## 1.文件位置

- 数据文件：`Data/citywalk_crowd_heat_realistic.json`
- 接入页面：`课程作业.html`
- 数据范围：上海徐汇区武康路、安福路、湖南路、淮海中路、复兴西路及外围轨道交通节点。

## 2.数据性质

这份数据是“现实空间节点驱动的可运行模拟热力数据”。

它不是官方实时客流计数，也不是手机信令数据。

它的现实依据来自三类内容：

- 真实POI与交通节点：地铁站、公交站、道路交叉口、餐饮、酒店、公共停留点等。
- 真实Citywalk热点：武康大楼、武康路街角、巴金故居、安福路商业段、上海图书馆站等。
- 课程系统需求：上午、午间、下午、傍晚、夜间五个时段需要能切换热力强弱。

## 3.为什么这样做

课程作业需要的是一个可解释、可演示、可运行的数字孪生原型。

真实城市客流通常需要运营商信令、景区闸机、摄像头识别或平台定位数据，这些数据一般不公开。

所以这里采用“真实节点+场景权重+时段模式”的方法，使热力图既贴合现实空间逻辑，又能在单HTMLCesium页面中直接运行。

## 4.JSON顶层结构

```json
{
  "metadata": {},
  "timeSlots": [],
  "points": [],
  "corridors": []
}
```

## 5.metadata

`metadata`记录数据名称、版本、空间范围、坐标系统、数据类型、说明与来源。

页面运行时不强依赖`metadata`，但汇报和后续文档可以引用。

## 6.timeSlots

`timeSlots`定义五个时段：

- `morning`：上午，09:00-11:00
- `noon`：午间，11:00-14:00
- `afternoon`：下午，14:00-17:00
- `evening`：傍晚，17:00-20:00
- `night`：夜间，20:00-22:30

当前HTML按钮已经有“傍晚时段”，后续可以继续加上午、午间、下午、夜间按钮。

## 7.points

`points`是热力图真正使用的节点数组。

每个节点包含：

- `id`：机器可读ID。
- `name`：界面显示名称。
- `category`：节点类别。
- `lng`：WGS84经度。
- `lat`：WGS84纬度。
- `radius`：基础影响半径。
- `slots`：不同时段的人流强度。
- `basis`：该点设置的现实依据。

## 8.slots强度规则

`slots`中的数值范围为0-100。

当前页面按三个等级渲染：

- 0-52：蓝色，舒适或低密。
- 53-72：黄色，活跃。
- 73-100：红色，拥挤。

## 9.当前HTML接入逻辑

页面启动时会读取：

```js
fetch("./Data/citywalk_crowd_heat_realistic.json")
```

读取成功后，页面会把`points`转换成原热力函数可用的数组结构：

```js
[name,lng,lat,intensity,{id,category,radius,basis}]
```

这样原来的热力绘制逻辑可以继续使用，不需要重写整个系统。

## 10.右侧人流面板

为了避免右侧栏被32个节点撑得过长，HTML中只显示当前强度最高的12个节点。

热力地图仍会绘制全部32个点。

这个策略符合之前分栏文件中“二级面板不阻断地图操作”的原则。

## 11.热力图绘制方式

当前热力图采用Cesium实体椭圆表达。

每个热力点由`ellipse`绘制：

- 半径=基础半径+强度加权。
- 材质颜色由强度决定。
- 透明度保持在0.28，避免遮挡建筑贴图。

## 12.后续可扩展方向

后续可以继续扩展三类功能：

- ECharts时段折线图：读取同一份`points`数据计算Top节点变化。
- 避拥挤路线：读取`corridors`中的低风险走廊推荐替代路线。
- AI语音跳转：识别“哪里人少”“哪里最拥挤”“带我避开武康大楼”等指令后调用同一份热力数据。

## 13.课堂汇报口径

汇报时建议这样表述：

本系统没有伪装成实时客流平台，而是采用真实开放地图POI、交通换乘节点和Citywalk热点构建人流热力样本库。系统通过五个时段的场景权重模拟街区客流变化，使热力开关、拥挤预警、避让路线和导览推荐具备可运行的数据基础。

