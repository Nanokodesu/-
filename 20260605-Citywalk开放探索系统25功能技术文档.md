# 武康路Citywalk开放式数字孪生导览与人流感知系统技术文档

001. 文档目标：把Word方案中的“开放式Citywalk平台+可选游客故事导览模块”转译为可实现的Cesium单HTML系统。
002. 文档目标：深度结合课程笔记中的Cesium Viewer、3DTiles、GeoJSON、KML、CZML、Entity、ECharts、热力图、卷帘、全景、粒子、鹰眼等代码能力。
003. 文档目标：先规划技术架构与功能组织，后续再按模块实现，避免边写边改导致界面互相遮挡。
004. 文档目标：至少规划25个功能，本版规划30个功能，按系统分栏与实现优先级组织。
005. 文档目标：保持课程作业可演示、可讲解、可落地，核心形式仍为`HTML+CSS+JavaScript`单文件。
006. 文档目标：当前项目主文件为`课程作业.html`，本技术文档不直接修改主HTML。
007. 文档范围：城市对象选择为上海徐汇/武康路Citywalk语境。
008. 文档范围：系统不是线性闯关，不强迫用户按固定剧情走。
009. 文档范围：系统默认是开放探索平台，故事导览只作为可选体验层。
010. 文档范围：AI语音入口不做泛聊天，而是把自然语言转换为Cesium操作命令。
011. 文档范围：分析模块不是后台大系统，而是课程展示所需的轻量人流、路线、节点、建筑、时段联动。
012. 文档范围：所有功能必须服务于“自由探索、智能指引、人流避让、城市记忆、课程技术展示”五个目标。
013. 文档约束：不引入Vite，不改为工程化项目。
014. 文档约束：继续使用本地`Build/Cesium/Cesium.js`与`Build/Cesium/Widgets/widgets.css`。
015. 文档约束：优先复用当前`Data`目录中的OSGB/3DTiles与插件资源。
016. 文档约束：如需新增插件，必须放入`Data/Plug-in`或同级明确目录，避免散乱。
017. 文档约束：界面继续采用深色半透明、轻量分栏、可折叠面板。
018. 文档约束：所有面板必须为工具型布局，不做大面积白色窗口。
019. 文档约束：任何功能打开后不能遮挡地图主要视野超过35%。
020. 文档约束：故事弹窗、全景窗口、图表窗口必须支持关闭、收起或切换标签。

## 一、系统定位

021. 系统名称：武康路Citywalk开放式数字孪生导览与人流感知系统。
022. 副标题：面向游客自由探索与城市记忆导览的Cesium三维街区平台。
023. 一句话表达：让游客既能自由走进武康路，也能在需要时跟随一封信、一段影像和一条街巷记忆进入城市故事。
024. 核心用户一：初访游客，需要快速理解路线、节点和人流情况。
025. 核心用户二：文学爱好者，需要以巴金故居等文学节点进入街区记忆。
026. 核心用户三：建筑漫步者，需要查看老洋房、街角建筑和历史空间。
027. 核心用户四：休闲游客，需要找安静路线、咖啡休憩点和停留空间。
028. 核心用户五：课程评委，需要看到Cesium课程代码的综合应用。
029. 产品层级一：开放探索层，作为默认主模式。
030. 产品层级二：游客故事导览层，作为可选沉浸模式。
031. 产品层级三：AI语音入口层，作为自然语言操作桥。
032. 产品层级四：分析决策层，作为人流、路线、节点和运营展示面板。
033. 产品层级五：课程技术展示层，作为功能开关、代码映射和汇报说明。
034. 设计原则一：先给用户自由探索能力，再给用户导览建议。
035. 设计原则二：先保证主地图清楚，再叠加分析图层。
036. 设计原则三：先实现可演示功能，再追加复杂算法。
037. 设计原则四：所有内容都应可在一个HTML中加载、开关、重置。
038. 设计原则五：每个功能都要有明确的Cesium对象、数据来源和界面入口。
039. 设计原则六：游客故事不能吞掉开放平台，只能作为一条可切换导览线路。
040. 设计原则七：功能组织必须让用户知道“我现在在哪、能做什么、下一步去哪里”。

## 二、课程笔记能力映射

041. 第一节课能力：理解数字孪生、智慧城市、CIM和期末作业要求。
042. 第二节课能力：Cesium基础Viewer、底图、地形、本地资源和Ion资源调用。
043. 第三节课能力：GeoJSON、KML、3DTiles、SHP转3DTiles、CZML动态场景。
044. 第五节课能力：Cesium坐标系统、鼠标事件、键盘事件、相机事件。
045. 第七节课能力：点、线、面、模型绘制与Entity属性编辑。
046. 第八节课能力：绘制工具、模型放置、动态形状和本地Cesium结构。
047. 第九节课能力：建筑交互渲染、分类设色、距离分类、同类建筑筛选、ECharts联动。
048. 第十节课能力：沿路种树、全景地图、面内种植、道路中心线插值与偏移。
049. 第十一次课程能力：分类设色调试、标签合并显示、GLSL着色器基础、透明度冲突处理。
050. 第十二次课程能力：流动线材质、云层、渐变、光环、雷达扫描等材质特效。
051. 第十三次课能力：粒子系统、下雪/天气/氛围特效、发射器和模型矩阵。
052. 第十四次课能力：双Cesium鹰眼图、比例尺、罗盘、导航控件、卷帘联动。
053. 第十五次课能力：CesiumHeatmap热力图、热力边界、热力点、颜色和透明度控制。
054. 第十六次课能力：HTML标题、段落、图像、列表、表单等页面信息组织。
055. 课程整合方向一：用3DTiles构建真实街区骨架。
056. 课程整合方向二：用GeoJSON/KML承载兴趣点、路线和街巷范围。
057. 课程整合方向三：用CZML承载故事导览、游客轨迹和时间回放。
058. 课程整合方向四：用ECharts承载路线偏好、人流时段和节点热度统计。
059. 课程整合方向五：用热力图承载游客密度、拍照热度和拥挤风险。
060. 课程整合方向六：用卷帘承载历史照片/旧地图与现实三维场景对比。
061. 课程整合方向七：用全景/视频承载节点记忆、街角影像和沉浸解释。
062. 课程整合方向八：用鼠标事件承载拾取、绘制、测量和用户标注。
063. 课程整合方向九：用相机事件承载自动飞行、故事镜头和语音跳转。
064. 课程整合方向十：用粒子/GLSL承载路线流光、故事氛围和夜游表达。
065. 课程整合判断：功能不应堆砌，必须组织成游客真实使用路径。
066. 课程整合判断：地图底板、兴趣路线、人流热力、故事导览是四个主轴。
067. 课程整合判断：绘制、量测、鹰眼、卷帘、全景是增强工具，不应抢主轴。
068. 课程整合判断：ECharts应和地图联动，不能只做静态装饰图。
069. 课程整合判断：语音入口应调用已有功能，不新建一套孤立系统。
070. 课程整合判断：所有课程能力最终服务于“开放探索”这一个产品口径。

## 三、界面分栏总布局

071. 页面采用“顶部状态栏+左侧探索栏+右侧分析栏+底部时间栏+地图浮层工具”的五区布局。
072. 顶部状态栏宽度占满页面，高度控制在54px以内。
073. 顶部状态栏用于系统标题、当前模式、快速搜索、AI语音输入和全局重置。
074. 左侧探索栏固定在左侧，宽度建议320px，可收起到48px。
075. 左侧探索栏用于兴趣筛选、路线选择、节点列表、故事导览和游客手账。
076. 右侧分析栏固定在右侧，宽度建议340px，可收起到48px。
077. 右侧分析栏用于ECharts、人流热力、路线对比、建筑统计和课程技术映射。
078. 底部时间栏高度建议68px，只在需要时间回放、故事进度或人流时段时展开。
079. 地图浮层工具放在地图左下或右下，用小图标按钮承载测量、绘制、图层、鹰眼、卷帘。
080. 故事导览卡片不常驻，开启故事模式后出现在底部中间，宽度不超过560px。
081. 全景/视频弹窗默认居中浮出，最大宽度不超过720px，必须有关闭按钮。
082. 图层管理面板使用弹出抽屉，不在一级页面常驻。
083. 课程技术映射面板放在右侧分析栏的“课程代码”标签页中。
084. 地图主体始终保留至少65%的可视区域。
085. 左侧与右侧面板同时展开时，中间地图仍可正常拖拽、缩放和拾取。
086. 功能入口按使用频率排序：路线、节点、人流、故事、语音、图层、工具、课程说明。
087. 顶部不放太多按钮，避免当前HTML中所有按钮挤在同一行。
088. 左栏使用标签页：探索、路线、故事、手账。
089. 右栏使用标签页：人流、建筑、图表、课程。
090. 底栏使用标签页：时段、故事进度、轨迹回放。
091. 地图浮层使用图标按钮：定位、量测、绘制、卷帘、鹰眼、清除。
092. 所有标签页切换只隐藏内容，不销毁Cesium实体。
093. 所有浮层开关要调用统一`setPanelState(panelId,open)`函数。
094. 所有功能开关要调用统一`toggleFeature(featureKey)`函数。
095. 所有临时图形要进入`tempEntities`或`featureEntities[featureKey]`，便于统一清除。
096. 所有路线实体要进入`routeEntities`，避免多条路线互相覆盖后无法管理。
097. 所有热力实体要进入`heatLayer`或`heatEntities`，避免重复添加。
098. 所有故事状态要进入`storyState`对象，避免故事步骤和自由探索互相干扰。
099. 所有图表实例要进入`charts`对象，避免重复初始化ECharts导致页面卡顿。
100. 所有弹窗文本要进入`contentLibrary`对象，后续可统一替换文案。

## 四、功能总表

101. 功能01：三维街区底座加载。
102. 功能02：本地Cesium与本地数据资源初始化。
103. 功能03：徐汇建筑3DTiles加载与定位。
104. 功能04：武康路兴趣点GeoJSON节点系统。
105. 功能05：开放探索兴趣筛选。
106. 功能06：多主题路线推荐。
107. 功能07：路线高亮与流动线材质。
108. 功能08：节点点击拾取与讲解卡片。
109. 功能09：建筑点击拾取与建筑信息面板。
110. 功能10：建筑分类设色。
111. 功能11：建筑距离圈选与邻近分析。
112. 功能12：人流热力图。
113. 功能13：时段人流切换。
114. 功能14：拥挤避让路线。
115. 功能15：ECharts路线与人流联动。
116. 功能16：游客故事导览模式。
117. 功能17：故事导览CZML轨迹。
118. 功能18：历史照片/旧地图卷帘对比。
119. 功能19：节点视频贴图或视频弹窗。
120. 功能20：全景节点漫游。
121. 功能21：AI语音意图识别与自动跳转。
122. 功能22：自然语言图层开关。
123. 功能23：空间绘制工具。
124. 功能24：距离/面积量测工具。
125. 功能25：停留服务半径分析。
126. 功能26：鹰眼小地图。
127. 功能27：粒子与夜游氛围。
128. 功能28：游客手账路线生成。
129. 功能29：截图导出与汇报模式。
130. 功能30：课程代码映射说明面板。
131. 功能分组一：底座与数据组，对应功能01至04。
132. 功能分组二：开放探索组，对应功能05至09。
133. 功能分组三：建筑分析组，对应功能10至11。
134. 功能分组四：人流分析组，对应功能12至15。
135. 功能分组五：故事与记忆组，对应功能16至20。
136. 功能分组六：AI交互组，对应功能21至22。
137. 功能分组七：空间工具组，对应功能23至26。
138. 功能分组八：表达输出组，对应功能27至30。
139. 必做等级A：功能01、02、03、04、05、06、08、12、15、16、21。
140. 必做等级B：功能07、09、10、13、14、18、23、24、30。
141. 加分等级C：功能11、17、19、20、25、26、27、28、29。
142. 实施原则：先实现A，再实现B，最后择优实现C。
143. 演示原则：每个分组至少有一个可以现场点击触发的功能。
144. 汇报原则：每个分组要能对应一段课程技术说明。
145. 防遮挡原则：任何新功能都必须先说明放在哪个栏位。
146. 防冲突原则：任何新图层都必须有唯一`featureKey`。
147. 防冲突原则：任何按钮都不能直接创建不可清除的实体。
148. 防冲突原则：任何定时器、语音监听、故事播放都必须有停止函数。
149. 防冲突原则：任何弹窗打开时不应阻断地图相机控制，除非是全景模式。
150. 防冲突原则：重置系统按钮必须恢复开放探索默认状态。

## 五、功能01三维街区底座加载

151. 功能定位：构建武康路/徐汇场景的基础三维地理环境。
152. 界面入口：顶部“聚焦街区”按钮与左栏“探索首页”。
153. 数据来源：高德/OSM/天地图底图、本地或Ion地形、本地3DTiles建筑。
154. Cesium对象：`Cesium.Viewer`、`UrlTemplateImageryProvider`、`CesiumTerrainProvider`。
155. 当前基础：主HTML已使用`new Cesium.Viewer("map")`初始化。
156. 实现路径：保留隐藏CesiumLogo、关闭时间轴、关闭默认按钮的轻量Viewer设置。
157. 实现路径：使用`viewer.imageryLayers.removeAll()`后添加稳定底图。
158. 实现路径：保留`depthTestAgainstTerrain=true`，但注意模型沉底时可临时关闭排查。
159. 实现路径：用`focusScene()`统一回到街区总览。
160. 防遮挡策略：底座功能没有面板，只更新状态栏。

## 六、功能02本地Cesium与本地数据资源初始化

161. 功能定位：确保课程作业离线或本地启动时可运行。
162. 界面入口：无独立入口，属于系统启动流程。
163. 数据来源：`Build/Cesium`、`Data/OSGB`、`Data/Plug-in`、`Data/GeoJSON`等。
164. Cesium对象：`window.CESIUM_BASE_URL`、脚本和样式链接。
165. 当前基础：主HTML已引用`./Build/Cesium/Cesium.js`。
166. 实现路径：统一把所有第三方插件路径写在HTML头部。
167. 实现路径：所有数据路径使用相对路径，不写死用户盘符。
168. 实现路径：初始化时执行`checkAssets()`，将关键资源加载状态写入右侧“课程”页。
169. 实现路径：加载失败只提示原因，不阻断其他功能。
170. 防遮挡策略：资源状态显示为小型列表，放在右栏课程页，不弹出大窗口。

## 七、功能03徐汇建筑3DTiles加载与定位

171. 功能定位：呈现实际徐汇建筑模型，形成课程作业最强视觉底座。
172. 界面入口：顶部“徐汇实景”按钮与图层管理。
173. 数据来源：`./Data/OSGB/徐汇区建筑/tileset.json`。
174. Cesium对象：`Cesium.Cesium3DTileset.fromUrl`、`modelMatrix`、`boundingSphere`。
175. 当前基础：主HTML已只加载建筑层。
176. 实现路径：保留`loadTileset([key,name,url,color,useBlend])`异步加载函数。
177. 实现路径：建筑层默认不设置`style color`，避免覆盖贴图。
178. 实现路径：定位参数集中放在`sceneConfig.center`，禁止散落多处。
179. 实现路径：加载完成后才允许建筑分类设色按钮启用。
180. 防遮挡策略：建筑加载状态只写入状态栏，不弹窗。

## 八、功能04武康路兴趣点GeoJSON节点系统

181. 功能定位：把文学、建筑、休憩、影像、拍照、人流节点组织为空间入口。
182. 界面入口：左栏“探索”标签页的节点分类。
183. 数据来源：可先使用JS数组，后续整理为`Data/GeoJSON/wukang_nodes.geojson`。
184. Cesium对象：`viewer.entities.add`、`GeoJsonDataSource.load`、`LabelGraphics`。
185. 当前基础：主HTML已有`nodes`数组和`addNodes()`函数。
186. 实现路径：节点字段包括`id`、`name`、`type`、`lng`、`lat`、`desc`、`storyKey`、`crowdScore`。
187. 实现路径：不同类型节点使用不同颜色，但色彩数量控制在5类以内。
188. 实现路径：节点标签使用距离显示条件，避免远景密集遮挡。
189. 实现路径：点击节点触发`showNodeCard(nodeId)`。
190. 防遮挡策略：节点说明进入左栏信息卡，不使用地图中央大弹窗。

## 九、功能05开放探索兴趣筛选

191. 功能定位：让游客按兴趣自由探索，而不是被故事线绑定。
192. 界面入口：左栏“探索”标签页顶部的兴趣筛选按钮组。
193. 数据来源：节点类型与路线类型。
194. Cesium对象：Entity显隐、DataSource显隐、标签显隐。
195. 实现路径：筛选项包括文学、建筑、休憩、影像、拍照、安静。
196. 实现路径：点击筛选后只显示相关节点，并同步右栏图表。
197. 实现路径：筛选不改变相机，除非用户点击“聚焦结果”。
198. 实现路径：筛选状态进入`uiState.activeFilters`数组。
199. 实现路径：取消筛选后恢复所有节点。
200. 防遮挡策略：筛选按钮放左栏，不在地图上堆按钮。

## 十、功能06多主题路线推荐

201. 功能定位：提供文学线、建筑线、安静线、咖啡休憩线、故事线五类路线。
202. 界面入口：左栏“路线”标签页。
203. 数据来源：路线节点序列、路线长度、拥挤风险、推荐指数。
204. Cesium对象：`PolylineGraphics`、`PolylineGlowMaterialProperty`。
205. 当前基础：主HTML已有文学线、建筑线、安静线。
206. 实现路径：扩展`routes`对象，加入`coffee`与`story`。
207. 实现路径：路线卡片显示名称、预计时间、拥挤风险、适合人群。
208. 实现路径：点击路线卡片调用`showRoute(routeKey)`。
209. 实现路径：同一时刻主路线只显示一条，备用路线可用虚线显示。
210. 防遮挡策略：路线列表在左栏，地图只显示线，不显示大面积面板。

## 十一、功能07路线高亮与流动线材质

211. 功能定位：让推荐路线具有视觉吸引力，体现第十二次课程的流动材质能力。
212. 界面入口：由路线推荐自动触发。
213. 数据来源：路线坐标数组或节点序列。
214. Cesium对象：自定义`FlowLineMaterialProperty`或`PolylineGlowMaterialProperty`。
215. 实现路径：基础版使用`PolylineGlowMaterialProperty`。
216. 实现路径：加分版注册`FlowLineType`材质，实现路线流光。
217. 实现路径：文学线用粉紫，建筑线用青蓝，安静线用绿色，拥挤避让线用蓝色。
218. 实现路径：路线开始与终点添加小点位标记。
219. 实现路径：故事路线播放时线条跟随CZML时间逐段显现。
220. 防遮挡策略：路线宽度控制在6至8px，不遮挡建筑贴图。

## 十二、功能08节点点击拾取与讲解卡片

221. 功能定位：点击节点后显示导览说明、推荐动作和关联课程功能。
222. 界面入口：地图节点点击。
223. 数据来源：节点资料库`contentLibrary.nodes`。
224. Cesium对象：`ScreenSpaceEventHandler`、`scene.pick`、Entity属性。
225. 当前基础：主HTML已有`bindPick()`。
226. 实现路径：识别`picked.id.type==="node"`后读取节点ID。
227. 实现路径：左栏信息卡显示标题、描述、当前人流、推荐路线按钮。
228. 实现路径：右栏图表同步突出该节点。
229. 实现路径：状态栏提示“已选中某某节点”。
230. 防遮挡策略：不用Cesium默认InfoBox，继续使用自定义左栏卡片。

## 十三、功能09建筑点击拾取与建筑信息面板

231. 功能定位：点击3DTiles建筑后展示建筑分类、相邻距离、路线关系。
232. 界面入口：地图建筑点击。
233. 数据来源：3DTiles属性、模拟建筑属性表、用户点击坐标。
234. Cesium对象：`scene.pick`、`Cesium3DTileFeature`、`getProperty`。
235. 实现路径：点击对象若不是节点，则判断是否为3DTileFeature。
236. 实现路径：读取建筑属性如`name`、`type`、`height`，没有则用坐标生成临时说明。
237. 实现路径：右栏“建筑”标签页显示建筑卡片。
238. 实现路径：提供“同类高亮”“邻近分析”“加入路线”三个小按钮。
239. 实现路径：高亮建筑注意透明度不要与边缘检测冲突。
240. 防遮挡策略：建筑信息不弹中央窗，进入右侧分析栏。

## 十四、功能10建筑分类设色

241. 功能定位：展示第九节课建筑交互渲染能力。
242. 界面入口：右栏“建筑”标签页。
243. 数据来源：建筑类型属性或模拟规则。
244. Cesium对象：`Cesium3DTileStyle`。
245. 实现路径：提供“按功能”“按高度”“按人流影响”三种设色方式。
246. 实现路径：设色前保存原始贴图状态，便于恢复。
247. 实现路径：设色透明度不要过低，避免贴图完全消失。
248. 实现路径：若当前要展示真实贴图，则只用边框或轻微色彩叠加。
249. 实现路径：重置时`tileset.style=undefined`或恢复默认样式。
250. 防遮挡策略：设色图例放右栏，不浮在地图中央。

## 十五、功能11建筑距离圈选与邻近分析

251. 功能定位：点击一个建筑或节点后，分析周边一定半径内的建筑与节点。
252. 界面入口：右栏“建筑”标签页的“邻近分析”按钮。
253. 数据来源：节点坐标、建筑中心点、用户设定半径。
254. Cesium对象：`CallbackProperty`、`EllipseGraphics`、距离计算。
255. 实现路径：以选中点为中心生成半透明圆形分析区。
256. 实现路径：计算半径内节点数量、建筑数量、人流点数量。
257. 实现路径：右栏显示统计结果。
258. 实现路径：可切换500m、800m、1000m三档半径。
259. 实现路径：圈选图形进入`featureEntities.nearby`。
260. 防遮挡策略：分析圆透明度控制在0.18以内。

## 十六、功能12人流热力图

261. 功能定位：用热力图显示游客密度、拍照热度和拥挤风险。
262. 界面入口：右栏“人流”标签页和顶部快速按钮。
263. 数据来源：模拟人流点、时段数据、节点热度。
264. Cesium对象：`CesiumHeatmap`或Entity椭圆模拟热力。
265. 当前基础：主HTML已有Entity椭圆模拟人流热力。
266. 实现路径：基础版继续用半透明椭圆表达热力。
267. 实现路径：增强版接入`CesiumHeatmap.js`生成连续热力图。
268. 实现路径：热力颜色为红色高拥挤、橙色中等、蓝色舒适。
269. 实现路径：热力图开关进入`featureState.heatmap`。
270. 防遮挡策略：热力透明度最大0.55，避免盖住3DTiles建筑。

## 十七、功能13时段人流切换

271. 功能定位：让人流分析从静态热度变成时间维度变化。
272. 界面入口：底部时间栏“上午、午后、傍晚、夜间”四段切换。
273. 数据来源：模拟时段热力数组。
274. Cesium对象：实体属性更新、ECharts更新。
275. 实现路径：定义`crowdByTime={morning,afternoon,evening,night}`。
276. 实现路径：切换时段时重绘热力实体。
277. 实现路径：同时更新右栏折线图和节点风险标签。
278. 实现路径：故事模式可自动切换到“傍晚”以增强氛围。
279. 实现路径：时间栏默认收起，打开人流分析后展开。
280. 防遮挡策略：底栏高度控制在68px，不覆盖左/右栏内容。

## 十八、功能14拥挤避让路线

281. 功能定位：根据人流热力为游客推荐更舒适的Citywalk路径。
282. 界面入口：左栏“路线”标签页和语音命令“哪里人少”。
283. 数据来源：路线拥挤风险、节点热度、时段数据。
284. Cesium对象：Polyline、热力实体、ECharts。
285. 实现路径：为每条路线计算`crowdRisk`。
286. 实现路径：选择风险最低路线作为避让路线。
287. 实现路径：拥挤区用红色热力显示，避让路线用蓝色流动线显示。
288. 实现路径：左栏解释为什么推荐该路线。
289. 实现路径：右栏显示路线风险对比条形图。
290. 防遮挡策略：避让解释最多三行，避免卡片变成长文本。

## 十九、功能15ECharts路线与人流联动

291. 功能定位：把地图交互结果转成可读图表，体现第九节课ECharts能力。
292. 界面入口：右栏“图表”标签页。
293. 数据来源：路线推荐指数、拥挤风险、节点停留热度、时段变化。
294. Cesium对象：不直接属于Cesium，但与地图实体联动。
295. 当前基础：主HTML已有路线推荐指数和拥挤风险图表。
296. 实现路径：初始化`charts.route=echarts.init(...)`。
297. 实现路径：点击路线时图表高亮对应柱。
298. 实现路径：点击图表柱时地图显示对应路线。
299. 实现路径：人流时段切换时折线图更新。
300. 防遮挡策略：图表只在右栏标签页显示，默认高度不超过220px。

## 二十、功能16游客故事导览模式

301. 功能定位：保留Word方案中的“一封没有寄出的信”，但只作为可选导览层。
302. 界面入口：左栏“故事”标签页、顶部“故事导览”按钮、语音命令。
303. 数据来源：故事脚本、故事节点、路线序列、历史影像素材。
304. Cesium对象：相机飞行、节点高亮、Polyline、弹窗卡片。
305. 当前基础：主HTML已有`startStory()`函数和故事文本区域。
306. 实现路径：故事状态进入`storyState={active,index,steps}`。
307. 实现路径：每一步包含标题、正文、目标节点、路线、图层动作。
308. 实现路径：故事卡片底部中间浮出，不影响左/右栏。
309. 实现路径：用户可随时暂停故事，回到开放探索模式。
310. 防遮挡策略：故事卡片宽度不超过560px，高度不超过160px。

## 二十一、功能17故事导览CZML轨迹

311. 功能定位：用CZML表达故事中的游客移动轨迹和时间过程。
312. 界面入口：故事模式中的“播放轨迹”按钮。
313. 数据来源：故事节点坐标和时间片段。
314. Cesium对象：`CzmlDataSource.load`、Clock、Timeline。
315. 实现路径：生成轻量CZML数组，不必单独写文件。
316. 实现路径：轨迹对象使用小人或圆点模型。
317. 实现路径：路线随时间移动，故事卡片随步骤更新。
318. 实现路径：播放结束后保留最终路线，可一键清除。
319. 实现路径：如果开启底部时间栏，显示故事进度而不是Cesium默认Timeline。
320. 防遮挡策略：轨迹图标小，不使用巨大模型。

## 二十二、功能18历史照片/旧地图卷帘对比

321. 功能定位：展示历史街区与当前三维街区的叠层记忆。
322. 界面入口：地图浮层“卷帘”按钮、故事步骤“老照片重现”。
323. 数据来源：历史影像、旧地图、当前底图。
324. Cesium对象：ImageryLayer splitDirection、scene.splitPosition。
325. 当前基础：主HTML已有`toggleSplit()`和`bindSplit()`。
326. 实现路径：当前底图设置为左侧，历史图层设置为右侧。
327. 实现路径：卷帘滑块只在开启时显示。
328. 实现路径：故事进入老照片步骤时自动开启卷帘。
329. 实现路径：关闭故事不强制关闭卷帘，由用户决定。
330. 防遮挡策略：滑块为细线，不产生大面板。

## 二十三、功能19节点视频贴图或视频弹窗

331. 功能定位：在街角节点展示短视频、历史片段或导览讲解。
332. 界面入口：节点讲解卡片中的“查看影像”按钮。
333. 数据来源：本地MP4、课程素材、外部视频截图。
334. Cesium对象：可选用Entity矩形视频材质，或HTML视频弹窗。
335. 实现路径：基础版使用HTML弹窗播放视频。
336. 实现路径：增强版在建筑立面附近生成视频矩形面。
337. 实现路径：视频只在用户点击后加载，避免初始化卡顿。
338. 实现路径：关闭视频时暂停并释放播放状态。
339. 实现路径：视频素材不足时可使用静态图+讲解文本替代。
340. 防遮挡策略：视频弹窗默认最大720px，必须可关闭。

## 二十四、功能20全景节点漫游

341. 功能定位：在关键节点提供街景式沉浸查看，结合第十节课全景地图内容。
342. 界面入口：节点讲解卡片中的“进入全景”按钮。
343. 数据来源：2:1全景图片或示意全景图。
344. Cesium对象：可用HTML全景容器，也可用球体贴图扩展。
345. 实现路径：基础版使用CSS/JS创建全景查看弹窗。
346. 实现路径：全景开启后暂停地图拾取，关闭后恢复地图操作。
347. 实现路径：全景节点坐标与地图节点联动。
348. 实现路径：全景内显示“返回三维地图”按钮。
349. 实现路径：全景模块作为加分项，不影响主功能。
350. 防遮挡策略：全景是独立模式，关闭后恢复原界面。

## 二十五、功能21AI语音意图识别与自动跳转

351. 功能定位：把游客自然语言转换为地点跳转、路线推荐、人流查询和故事启动。
352. 界面入口：顶部语音输入框和“执行”按钮。
353. 数据来源：意图词典、节点别名、路线别名、图层别名。
354. Cesium对象：`viewer.camera.flyTo`、Entity高亮、图层开关。
355. 当前基础：主HTML已有`runCommand()`文本命令入口。
356. 实现路径：先做文本输入版本，再接Web Speech API语音识别。
357. 实现路径：意图类型包括地点、路线、人流、故事、历史、图层、工具。
358. 实现路径：例如“带我去巴金故居”匹配地点意图。
359. 实现路径：例如“哪里人少”匹配拥挤避让意图。
360. 防遮挡策略：语音结果写入顶部状态和左栏卡片，不弹巨大对话框。

## 二十六、功能22自然语言图层开关

361. 功能定位：让用户通过语音或文本打开/关闭热力、卷帘、建筑、节点、路线。
362. 界面入口：AI语音输入与图层管理面板。
363. 数据来源：`featureRegistry`功能注册表。
364. Cesium对象：ImageryLayer.show、DataSource.show、Entity.show、Primitive.show。
365. 实现路径：建立`featureRegistry={heatmap:{on,off},split:{on,off},nodes:{on,off}}`。
366. 实现路径：语音命令不直接操作实体，而是调用注册表方法。
367. 实现路径：支持“打开人流热力”“关闭故事路线”“显示建筑模型”等。
368. 实现路径：无法识别时给出可用命令提示。
369. 实现路径：图层状态同步到图层管理抽屉。
370. 防遮挡策略：图层抽屉默认收起，只在用户点击时展开。

## 二十七、功能23空间绘制工具

371. 功能定位：让用户在地图上标注停留半径、观察片区、路线补充点。
372. 界面入口：地图浮层“绘制”按钮或左栏工具区。
373. 数据来源：用户鼠标点击。
374. Cesium对象：`ScreenSpaceEventHandler`、Point、Ellipse、Rectangle、Polyline。
375. 当前基础：主HTML已有停留半径和观察片区绘制。
376. 实现路径：把绘制模式统一为`drawState.mode`。
377. 实现路径：支持点、线、矩形、圆形四类绘制。
378. 实现路径：绘制完成后进入`tempEntities`。
379. 实现路径：提供一键清除临时标注。
380. 防遮挡策略：绘制工具提示只写状态栏，不弹说明窗口。

## 二十八、功能24距离/面积量测工具

381. 功能定位：展示第五节课坐标与鼠标事件能力，并用于街区尺度理解。
382. 界面入口：地图浮层“量测”按钮。
383. 数据来源：用户点击点序列。
384. Cesium对象：Cartesian3.distance、Cartographic、Polyline、Polygon。
385. 实现路径：距离量测为多段线累加距离。
386. 实现路径：面积量测为多边形面积近似计算。
387. 实现路径：测量结果显示在左栏或鼠标旁小标签。
388. 实现路径：量测实体进入`featureEntities.measure`。
389. 实现路径：切换工具时自动结束上一个绘制状态。
390. 防遮挡策略：测量标签短文本，避免地图标签堆叠。

## 二十九、功能25停留服务半径分析

391. 功能定位：帮助游客判断某节点周边是否适合停留、休息和避让。
392. 界面入口：节点卡片“分析周边停留”按钮。
393. 数据来源：节点坐标、休憩点、人流点、建筑/街巷数据。
394. Cesium对象：Ellipse、Entity筛选、ECharts。
395. 实现路径：以节点为中心绘制300m或500m服务半径。
396. 实现路径：统计半径内休憩点数量、人流风险和相邻路线。
397. 实现路径：给出“适合停留/不建议久留/适合绕行”的结论。
398. 实现路径：右栏显示雷达图或评分条。
399. 实现路径：结论要解释依据，避免只给分数。
400. 防遮挡策略：服务半径为浅蓝透明圈，不盖住主路线。

## 三十、功能26鹰眼小地图

401. 功能定位：帮助用户理解自己在街区整体中的位置。
402. 界面入口：地图浮层“鹰眼”按钮。
403. 数据来源：主视图相机状态。
404. Cesium对象：第二个`Cesium.Viewer`、camera.changed监听。
405. 实现路径：右下角创建小型`eagleEye`容器。
406. 实现路径：鹰眼图禁用鼠标交互，只跟随主相机。
407. 实现路径：鹰眼默认关闭，打开后占用右下角小区域。
408. 实现路径：如果性能压力大，鹰眼只显示底图和范围框。
409. 实现路径：关闭鹰眼时销毁或隐藏容器。
410. 防遮挡策略：鹰眼尺寸不超过220px×160px。

## 三十一、功能27粒子与夜游氛围

411. 功能定位：用于故事导览、夜游模式和路线氛围增强。
412. 界面入口：故事模式、地图浮层“氛围”按钮。
413. 数据来源：粒子纹理、故事状态、夜游开关。
414. Cesium对象：`Cesium.ParticleSystem`、BoxEmitter、CircleEmitter、modelMatrix。
415. 实现路径：开放探索默认不开启粒子，避免干扰地图阅读。
416. 实现路径：故事模式可开启少量紫色光点或路线微光。
417. 实现路径：夜游模式降低底图亮度，增强路线流光。
418. 实现路径：粒子数量要少，生命周期短，避免浏览器卡顿。
419. 实现路径：关闭故事时同步关闭粒子系统。
420. 防遮挡策略：粒子只作为氛围，不覆盖节点和热力判断。

## 三十二、功能28游客手账路线生成

421. 功能定位：让游客游后生成个人路线摘要，强化开放平台传播性。
422. 界面入口：左栏“手账”标签页。
423. 数据来源：用户选择路线、访问节点、截图、输入一句话。
424. Cesium对象：相机截图、Entity路径、HTML表单。
425. 实现路径：记录用户点击过的节点ID。
426. 实现路径：生成路线标题、节点序列、推荐语和拥挤避让说明。
427. 实现路径：提供“生成手账卡片”按钮。
428. 实现路径：HTML卡片可以后续截图导出。
429. 实现路径：不做真实账号系统，只做本地展示。
430. 防遮挡策略：手账卡片在左栏展示，完整预览再弹出。

## 三十三、功能29截图导出与汇报模式

431. 功能定位：方便课程展示和最终答辩录屏。
432. 界面入口：顶部“汇报模式”或地图浮层“截图”按钮。
433. 数据来源：当前Cesium画布和当前UI状态。
434. Cesium对象：`viewer.scene.canvas.toDataURL`。
435. 实现路径：汇报模式隐藏左/右栏，只保留标题、图例、地图。
436. 实现路径：截图按钮导出当前画面。
437. 实现路径：提供“恢复操作模式”按钮。
438. 实现路径：截图前可临时提高地图清晰度，但不强制。
439. 实现路径：导出失败时提示浏览器安全限制。
440. 防遮挡策略：汇报模式本身就是去遮挡模式。

## 三十四、功能30课程代码映射说明面板

441. 功能定位：让评委看到每个系统功能对应哪节课的知识点。
442. 界面入口：右栏“课程”标签页。
443. 数据来源：课程笔记和功能注册表。
444. Cesium对象：不直接依赖Cesium，属于说明面板。
445. 实现路径：用表格列出功能、课程来源、代码对象、演示入口。
446. 实现路径：例如热力图对应第十五次课，建筑设色对应第九节课。
447. 实现路径：点击表格行可触发对应演示功能。
448. 实现路径：课程映射内容必须简洁，避免占据主视图。
449. 实现路径：最终答辩时可作为讲解提词器。
450. 防遮挡策略：只在右栏显示，不弹大文档窗口。

## 三十五、核心数据结构

451. 数据结构一：`sceneConfig`保存中心点、默认高度、默认视角和模型偏移。
452. 数据结构二：`featureRegistry`保存每个功能的开关方法和状态。
453. 数据结构三：`uiState`保存当前打开的栏位、标签页、筛选项和模式。
454. 数据结构四：`nodes`保存兴趣点、故事点、休憩点和影像点。
455. 数据结构五：`routes`保存路线名称、节点序列、颜色、风险、推荐指数。
456. 数据结构六：`crowdByTime`保存不同时间段的人流点。
457. 数据结构七：`storyState`保存故事是否开启、当前步骤、计时器和轨迹。
458. 数据结构八：`featureEntities`保存各功能创建的临时实体。
459. 数据结构九：`loadedTilesets`保存已加载的3DTiles图层。
460. 数据结构十：`charts`保存ECharts实例，避免重复初始化。
461. 建议字段：`nodes[].aliases`用于语音识别地点别名。
462. 建议字段：`nodes[].tags`用于兴趣筛选。
463. 建议字段：`nodes[].crowdScore`用于热力和避让。
464. 建议字段：`nodes[].media`用于视频或全景素材。
465. 建议字段：`routes[].path`保存节点ID序列。
466. 建议字段：`routes[].riskByTime`保存分时段风险。
467. 建议字段：`routes[].storyStep`标记故事路线对应步骤。
468. 建议字段：`featureRegistry[].panel`说明功能归属栏位。
469. 建议字段：`featureRegistry[].course`说明课程来源。
470. 建议字段：`featureRegistry[].priority`说明实现优先级。

## 三十六、单HTML实现结构建议

471. HTML区域一：`#map`作为Cesium主容器。
472. HTML区域二：`#topBar`作为顶部状态栏。
473. HTML区域三：`#leftPanel`作为左侧探索栏。
474. HTML区域四：`#rightPanel`作为右侧分析栏。
475. HTML区域五：`#bottomTimeline`作为底部时间栏。
476. HTML区域六：`#mapTools`作为地图浮层按钮组。
477. HTML区域七：`#storyCard`作为故事导览浮层。
478. HTML区域八：`#mediaModal`作为视频/全景弹窗。
479. CSS区域一：全局变量，包括面板背景、边框、文字色、强调色。
480. CSS区域二：地图和Cesium容器，保证全屏。
481. CSS区域三：顶部、左栏、右栏、底栏定位。
482. CSS区域四：标签页、卡片、按钮、输入框和图表容器。
483. CSS区域五：折叠状态、隐藏状态、响应式状态。
484. JS区域一：配置与数据。
485. JS区域二：初始化Viewer和资源加载。
486. JS区域三：节点、路线、热力、建筑、故事等功能函数。
487. JS区域四：UI状态管理和事件绑定。
488. JS区域五：ECharts和媒体弹窗。
489. JS区域六：语音/文本命令解析。
490. JS区域七：重置、清除、截图、汇报模式。

## 三十七、防遮挡与互不干扰规则

491. 规则一：一级界面只保留顶部、左栏、右栏、地图浮层，不再堆多个独立大窗口。
492. 规则二：左栏负责“我要去哪”，右栏负责“为什么这样推荐”。
493. 规则三：底栏只负责时间变化，不放普通按钮。
494. 规则四：故事卡片只在故事模式出现。
495. 规则五：视频和全景只在用户主动点击时出现。
496. 规则六：图层管理、课程说明、手账预览都使用标签页或抽屉，不常驻地图中央。
497. 规则七：地图上的标签必须设置距离显示条件。
498. 规则八：热力图必须控制透明度，不能完全盖住建筑贴图。
499. 规则九：建筑分类设色必须有恢复贴图按钮。
500. 规则十：路线只显示当前主路线，其他路线通过列表选择。
501. 规则十一：临时绘制与量测必须可一键清除。
502. 规则十二：语音命令只触发已有功能，不直接创建无法管理的新图层。
503. 规则十三：故事模式开启时不清除开放探索节点。
504. 规则十四：故事模式结束后保留用户选择，除非点击重置。
505. 规则十五：鹰眼图默认关闭，防止右下角长期遮挡。
506. 规则十六：汇报模式需要一键隐藏操作面板。
507. 规则十七：所有按钮需要有明确状态反馈。
508. 规则十八：所有加载失败需要局部降级，不阻断整页。
509. 规则十九：所有数组数据都要集中定义，减少后续改动成本。
510. 规则二十：所有功能完成后都要在课程映射面板登记。

## 三十八、推荐实施顺序

511. 第一步：重构页面分栏骨架，但不改变现有Cesium加载逻辑。
512. 第二步：把顶部按钮迁移到左栏、右栏和地图浮层，减少一级按钮拥挤。
513. 第三步：建立`featureRegistry`、`uiState`、`featureEntities`三个管理对象。
514. 第四步：整理`nodes`与`routes`数据，补齐咖啡休憩线和故事线。
515. 第五步：完善节点拾取、路线推荐和左栏讲解卡片。
516. 第六步：完善人流热力、时段切换和ECharts联动。
517. 第七步：完善故事导览卡片和故事步骤控制。
518. 第八步：接入语音/文本命令解析，让语音能调用路线、人流、故事和卷帘。
519. 第九步：增加建筑分类设色和建筑点击信息面板。
520. 第十步：增加绘制、量测、停留半径等空间工具。
521. 第十一步：增加卷帘、全景、视频、粒子等展示增强功能。
522. 第十二步：增加课程代码映射说明和汇报模式。
523. 第十三步：统一检查所有功能的关闭、清除和重置逻辑。
524. 第十四步：录制演示脚本，确保五分钟内能展示核心功能。
525. 第十五步：根据电脑性能选择是否保留鹰眼、粒子、全景等加分功能。

## 三十九、最终演示动线

526. 演示一：打开系统，聚焦徐汇建筑3DTiles底座。
527. 演示二：使用左栏筛选文学节点，点击巴金故居说明卡。
528. 演示三：切换建筑线，展示路线高亮和ECharts推荐指数。
529. 演示四：打开人流热力，切换午后时段，展示拥挤节点。
530. 演示五：输入“哪里人少”，系统显示安静避让线。
531. 演示六：开启“一封没有寄出的信”故事导览。
532. 演示七：故事进入老照片步骤，自动打开历史卷帘。
533. 演示八：点击影像节点，打开视频或全景弹窗。
534. 演示九：点击建筑，右栏显示建筑分类与同类高亮。
535. 演示十：绘制停留半径，展示节点周边服务范围。
536. 演示十一：打开课程映射面板，说明每个功能来自哪节课。
537. 演示十二：切换汇报模式，隐藏面板，输出课程展示画面。
538. 演示重点：系统不是单一故事路线，而是开放探索平台。
539. 演示重点：故事只是游客主动开启的沉浸导览模式。
540. 演示重点：人流分析真正影响路线推荐，而不是装饰热力图。
541. 演示重点：AI语音真正触发Cesium动作，而不是普通聊天框。
542. 演示重点：课程笔记功能被组织成可理解的游客体验。
543. 演示重点：三维建筑、路线、热力、卷帘、图表、语音能互相联动。
544. 演示重点：分栏设计让功能互不遮挡。
545. 演示重点：所有功能可开关、可恢复、可重置。

## 四十、结论

546. 本系统应以“开放探索”为默认产品逻辑。
547. 本系统应以“故事导览”为体验亮点，而不是唯一流程。
548. 本系统应以“人流感知与路线避让”为技术核心，而不是普通景点展示。
549. 本系统应以“课程代码综合应用”为答辩支撑，而不是单纯美化界面。
550. 本系统应以“分栏与状态管理”为后续实现基础，避免功能越加越乱。
551. 本技术文档规划了30个功能，满足至少25个功能的要求。
552. 本技术文档按底座、探索、建筑、人流、故事、AI、工具、输出八组组织。
553. 本技术文档明确了顶部、左栏、右栏、底栏和地图浮层的职责。
554. 本技术文档明确了每个功能的入口、数据来源、Cesium对象、实现路径和防遮挡策略。
555. 后续实现时应先完成A类必做功能，再加入B类推荐功能，最后选择C类加分功能。
556. 后续实现时不应一次性重写整页，而应先搭建状态管理和分栏，再逐步迁移现有功能。
557. 后续实现时应保留当前建筑层加载成果，避免重新引入多层覆盖问题。
558. 后续实现时应对每个新增功能设置唯一`featureKey`。
559. 后续实现时应把新增实体都纳入统一数组或对象管理。
560. 后续实现完成后，可基于本文档生成答辩讲稿和系统演示脚本。
