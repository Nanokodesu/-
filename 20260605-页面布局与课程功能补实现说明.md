# 20260605-页面布局与课程功能补实现说明

## 本轮目标

本轮针对当前Cesium单HTML项目做两件事：

1. 将页面从“多个窗口直接铺在一级页面”调整为“全屏三维场景+侧边Dock+可收纳抽屉+底部指标卡”的结构。
2. 遍历课程笔记与本地`Data`资源，将此前只写在说明文档中的课程功能补成可点击、可运行的函数入口。

## 当前主项目位置

```text
E:/321/nano/学习中/-数字孪生与智慧城市/-/
```

该目录是当前唯一推荐运行目录，入口文件为：

```text
index.html
```

旧的空目录：

```text
E:/321/nano/学习中/-数字孪生与智慧城市/作业/结课作业方案/citywalk-cesium-local
```

已确认为空并删除，避免打开错误工程。

## 布局调整

页面参考了Cesium官方示例库和常见智慧城市三维大屏的组织方式，采用以下层级：

| 区域 | 当前功能 | 设计目的 |
| :--- | :--- | :--- |
| 顶部栏 | 模式切换、时段切换、核心操作 | 只保留高频入口，避免遮挡场景 |
| 左侧Dock | 导、析、课、收起 | 用短按钮打开对应抽屉 |
| 导览抽屉 | AI语音、路线、POI、故事导览 | 面向游客使用 |
| 分析抽屉 | 人流状态、ECharts图表 | 面向汇报和运营分析 |
| 课程抽屉 | 3DTiles、GeoJSON、KML、CZML、粒子、淹没、可视域等 | 面向课程功能展示 |
| 底部状态卡 | 当前模式、POI数量、临时图层、已启用课程功能 | 保留系统反馈但减少占屏 |

## 已补实现的课程功能

| 功能 | 对应函数 | 本地资源/代码基础 |
| :--- | :--- | :--- |
| 本地Cesium调用 | 页面头部脚本 | `Build/Cesium/Cesium.js`、`Widgets/widgets.css` |
| ECharts联动 | `initCharts()` | `Data/Plug-in/echarts-6.0.0/.../echarts.min.js` |
| 3DTiles加载 | `loadLocalTileset()` | `Data/Slice_shp/tileset.json` |
| GeoJSON加载 | `loadGeoJsonLayers()` | `Road_bjfu.geojson`、`Grass_bjfu.geojson` |
| KML加载 | `loadKmlLayer()` | `Data/KML/BJFU_SLA.kml` |
| CZML动态轨迹 | `loadCzmlVehicle()` | 运行时CZML+`GroundVehicle.glb` |
| GLB模型种植 | `addPlantingLayer()` | `CommonTree_*.glb` |
| 全景入口 | `showPanorama()` | `Data/quanjing.jpg` |
| 插件热力图 | `addHeatmapPluginLayer()` | `CesiumHeatmap.js` |
| 导航控件 | `toggleNavigation()` | `viewerCesiumNavigationMixin.min.js` |
| 可视域视锥 | `addViewshedDemo()` | Entity轻量视锥表达 |
| 淹没分析 | `addFloodDemo()` | `CallbackProperty`动态水位 |
| 粒子天气 | `toggleWeather()` | `ParticleSystem`+`Rain.png` |
| 等高线/雾效 | `toggleContour()` | `ElevationContour`材质+`Fog` |
| 雷达扫描 | `addRadarScan()` | `CallbackProperty`+`Radar.png` |
| 卷帘对比 | `toggleHistorySplit()` | `SplitDirection`思想+滑块 |
| 交互绘制 | `enableDraw()` | `ScreenSpaceEventHandler` |

## 注意事项

- 课程功能图层中的部分资源来自北京林业大学或课堂测试数据，只作为功能验证层；最终可以替换为武康路真实POI、路网、人流和街景媒体。
- 当前仍保持单HTML写法，符合课程原有`HTML+CSS+JS`组织方式。
- 若直接双击HTML时资源加载异常，建议在该目录启动本地静态服务后访问。

## 推荐打开方式

```bash
python -m http.server 8765
```

浏览器访问：

```text
http://127.0.0.1:8765/index.html
```
